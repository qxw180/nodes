# 常用单词
+ `Reference`：*参考*、涉及、*引用*
+ `Variable`：*变量*、可变的的
+ `Declarations`：声明
+ `Grammar`：语法
+ `Statements`：声明、报告、陈述句；*语句*
+ `Comments`：评论、*注释*、注解
+ `Identifiers`：标识符、变量的名称称为Identifiers
+ `Literals`：字面量、字面值
+ ``：
+ ``：
+ ``：
+ ``：
+ ``：
+ ``：
+ ``：
+ ``：
+ ``：
+ ``：
+ ``：
+ ``：
+ ``：
+ ``：
+ ``：
+ ``：
+ ``：
+ ``：
+ ``：
+ ``：
+ ``：
+ ``：
+ ``：